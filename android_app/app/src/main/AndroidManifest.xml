<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.not404.antifake">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".URLtool"
        android:targetSandboxVersion="1"
        android:allowBackup="true"
        android:usesCleartextTraffic="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AntiFake"
        tools:targetApi="31">
        <activity
            android:name=".FinalResponse"
            android:exported="false"
            android:label="@string/title_activity_final_response"
            android:theme="@style/Theme.AntiFake.NoActionBar" />
        <activity
            android:name=".QuestionAndQuery"
            android:exported="false"
            android:label="@string/title_activity_question_and_query"
            android:theme="@style/Theme.AntiFake.NoActionBar" />
        <activity
            android:name=".Loading"
            android:exported="false"
            android:label="@string/title_activity_loading"
            android:theme="@style/Theme.AntiFake.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AntiFake.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>